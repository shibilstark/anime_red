enum FailureType {
  client,
  server,
  unknown,
  internet,
}

enum MediaType {
  image,
  video,
}

enum SearchMediaSize {
  large,
  medium,
  small,
}

enum StreamingQuality {
  defaultQuality,
  backupQuality,
  quality360p,
  quality480p,
  quality720p,
  quality1080p,
}
